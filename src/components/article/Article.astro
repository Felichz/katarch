---
/**
 * KatArch article renderer — one component, two languages.
 * Content comes entirely from src/data/article/{es,en}.ts; this file owns
 * layout, interaction affordances and modals. Editorials rules encoded here:
 * concepts and ADRs open contextual modals (never spoilers of later
 * sections), and every modal locks page scroll.
 */
import type { ArticleContent } from '../../data/article/types';
import { CONCEPTS } from '../../data/article/concepts';
import { DECISIONS } from '../../data/article/decision-map';
import { ORIGINAL_DOCS } from '../../data/article/original-docs';
import BlockRenderer from './BlockRenderer.astro';

interface Props {
  content: ArticleContent;
}

const { content } = Astro.props;
const lang = content.lang;
const isES = lang === 'es';

const ui = {
  close: isES ? 'Cerrar' : 'Close',
} as const;

const conceptEntries = Object.entries(CONCEPTS);
const usedConcepts = new Set<string>();
const htmlSources: string[] = [...content.heroParagraphs];
for (const section of content.sections) {
  for (const block of section.blocks) {
    if (block.type === 'p' || block.type === 'list') {
      const htmls =
        block.type === 'p' ? [block.html] : (block as { items: string[] }).items;
      htmlSources.push(...htmls);
    } else if (block.type === 'cards') {
      htmlSources.push(...block.cards.map((c) => c.html));
    } else if (block.type === 'callout') {
      htmlSources.push(block.html);
    } else if (block.type === 'table') {
      htmlSources.push(...block.rows.flat());
    }
  }
}
for (const h of htmlSources) {
  for (const m of h.matchAll(/data-concept="([^"]+)"/g)) usedConcepts.add(m[1]);
}
const modalConcepts = conceptEntries.filter(([id]) => usedConcepts.has(id));

// Original-doc modals: only for docs actually referenced via data-doc in prose
// or from the decision cards (their ADR chips open adr-XXX doc modals)
const usedDocIds = new Set<string>();
for (const h of htmlSources) {
  for (const m of h.matchAll(/data-doc="([^"]+)"/g)) usedDocIds.add(m[1]);
}
for (const d of DECISIONS) for (const ref of d.adrs) usedDocIds.add(`adr-${ref.id}`);
const usedDocs = ORIGINAL_DOCS.filter((d) => usedDocIds.has(d.id));
const uiDocKicker = isES ? 'Documento original · ArchColider' : 'Original document · ArchColider';
const uiDocOpen = isES ? 'Ver archivo original en el repo de ArchColider' : 'Open the original file in the ArchColider repo';

// The world bench: the six visual worlds, thumb labels per edition
const WORLDS = [
  { id: 'memo', es: 'Memo', en: 'Memo' },
  { id: 'botteghe', es: 'Revista azul', en: 'Quarterly' },
  { id: 'bench', es: 'Montaje', en: 'Bench' },
  { id: 'plate', es: 'Placa', en: 'Plate' },
  { id: 'tensegrity', es: 'Tenségrita', en: 'Tensegrity' },
  { id: 'datamatics', es: 'Datos B/N', en: 'Data B/W' },
];
---

<!-- Concept modal shells (only those actually referenced in the article) -->
{modalConcepts.map(([id, c]) => (
  <dialog id={`concept-${id}`} class="modal-shell" aria-labelledby={`concept-${id}-title`}>
    <div class="modal-card">
      <button class="modal-close" data-close-modal type="button" aria-label={ui.close}>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
      </button>
      <h3 id={`concept-${id}-title`} class="modal-title">{c.title[lang]}</h3>
      <div class="modal-body set-prose" set:html={c.body[lang]} />
    </div>
  </dialog>
))}

<!-- Original-doc modal shells (only those actually referenced in the article) -->
{usedDocs.map((d) => (
  <dialog class="modal-shell" data-doc-dialog={d.id} aria-labelledby={`doc-${d.id}-title`}>
    <div class="modal-card modal-card--doc">
      <button class="modal-close" data-close-modal type="button" aria-label={ui.close}>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
      </button>
      <p class="figure-guide-kicker">{uiDocKicker}</p>
      <h3 class="modal-title" id={`doc-${d.id}-title`}>
        <span data-doc-title-es hidden={!isES}>{d.titleEs}</span>
        <span data-doc-title-en hidden={isES}>{d.titleEn}</span>
      </h3>
      <div class="doc-footer">
        {d.htmlEs && (
          <span class="doc-lang-note" data-doc-note hidden={!isES}>
            {isES
              ? 'Traducción del artículo; el original en inglés es el documento de referencia.'
              : 'Translation by this project; the English original is the reference document.'}
          </span>
        )}
        <div class="doc-footer-actions">
          {/* The toggle is a Spanish-reader affordance: the English edition
              always shows the original document, with no switch. */}
          {isES && d.htmlEs && (
            <button class="doc-lang-toggle" data-doc-lang-toggle type="button">
              Ver original en inglés
            </button>
          )}
          <a
            class="doc-original-link"
            href={`https://github.com/TheKataLog/ArchColider/blob/master/${encodeURI(d.file)}`}
            target="_blank"
            rel="noopener noreferrer"
          >
            {uiDocOpen}
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17 17 7M8 7h9v9"/></svg>
          </a>
        </div>
      </div>
      {d.htmlEs ? (
        <>
          <div class="doc-prose" data-doc-content-es hidden={!isES} set:html={d.htmlEs} />
          <div class="doc-prose" data-doc-content-en hidden={isES} set:html={d.html} />
        </>
      ) : (
        <div class="doc-prose" set:html={d.html} />
      )}
    </div>
  </dialog>
))}

<!-- Lens stage: full-screen plate so the lens can reach every corner -->
<div class="lens-stage" data-lens-stage hidden>
  <div class="lens-stage-plate" data-lens-plate>
    <img class="lens-stage-img" data-lens-img src="" alt="" draggable="false" />
    <div class="figure-lens" data-lens-lens hidden></div>
    <span class="figure-lens-hint" data-lens-pill hidden></span>
  </div>
  <div class="lens-stage-bar">
    <span class="lens-stage-help">
      {isES
        ? 'scroll = zoom · clic en cualquier lado = cerrar · Esc = cerrar'
        : 'scroll = zoom · click anywhere = close · Esc = close'}
    </span>
    <button class="lens-stage-close" data-lens-close type="button">
      {isES ? 'Cerrar' : 'Close'}
      <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" aria-hidden="true"><path d="M18 6 6 18M6 6l12 12"/></svg>
    </button>
  </div>
</div>

    

<div class="article-shell">
  <!-- Collapsible TOC rail: collapsed dots by default, expands to the full index -->
  <aside class="toc-rail" id="toc-rail" aria-label={content.tocTitle}>
    <div class="toc-card" data-toc-card>
      <button
        class="toc-expand"
        data-toc-expand
        type="button"
        aria-label={isES ? 'Expandir índice' : 'Expand contents'}
        title={isES ? 'Expandir índice' : 'Expand contents'}
      >
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 5l7 7-7 7"/></svg>
      </button>
      <nav class="toc-full" data-toc-full hidden>
        <div class="toc-full-head">
          <p class="toc-heading">{content.tocTitle}</p>
          <button
            class="toc-collapse"
            data-toc-collapse
            type="button"
            aria-label={isES ? 'Colapsar índice' : 'Collapse contents'}
            title={isES ? 'Colapsar a barra de puntos' : 'Collapse to dot rail'}
          >
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 19l-7-7 7-7"/></svg>
          </button>
        </div>
        {content.sections.map((s) => (
          <a href={`#${s.id}`} class="toc-item" data-toc={s.id}>
            <span class="toc-phase">{s.phase}</span>
            <span class="toc-title">{s.title}</span>
          </a>
        ))}
      </nav>
      <nav class="toc-dots" data-toc-dots>
        {content.sections.map((s) => (
          <a
            href={`#${s.id}`}
            class="toc-dot"
            data-toc={s.id}
            data-tooltip={s.title}
            aria-label={s.title}
          >
            <span class="dot-indicator" />
          </a>
        ))}
      </nav>
    </div>
    <div class="toc-tooltip" data-toc-tooltip hidden></div>
  </aside>

  <article class="article">
    <!-- Hero -->
    <header class="hero">
      <div class="memo-masthead">
        <p class="mast-row"><span class="mast-key">{isES ? 'Documento' : 'Document'}</span><span class="mast-val">KatArch · {isES ? 'reconstrucción de un caso real' : 'a real case, reconstructed'}</span></p>
        <p class="mast-row"><span class="mast-key">{isES ? 'Serie' : 'Series'}</span><span class="mast-val">KATA/2020-FALL · Farmacy Food · O&#8217;Reilly Software Architecture Kata</span></p>
        <p class="mast-row"><span class="mast-key">{isES ? 'Estado' : 'Status'}</span><span class="mast-val">{isES ? 'Aceptado · fuentes: repos públicos de los equipos' : 'Accepted · sources: the teams public repos'}</span></p>
      </div>
      <h1 class="hero-title">
        {content.heroTitleA}
        <span class="hero-title-b">{content.heroTitleB}</span>
      </h1>
      <div class="hero-prose set-prose">
        {content.heroParagraphs.map((p) => <p set:html={p} />)}
      </div>
    </header>

    <!-- World bench: hover previews the world live, click applies it -->
        <section class="world-bench world-picker" data-world-bench aria-label={isES ? 'Estilo visual' : 'Visual style'} role="radiogroup">
          <p class="bench-legend">
            <span class="bench-title">{isES ? 'Estilo visual' : 'Visual style'}</span>
            <span class="bench-hint">{isES ? 'pasá el mouse para ver · clic para aplicar' : 'hover to preview · click to apply'}</span>
          </p>
          <div class="bench-row">
            {WORLDS.map((w) => (
              <button class="world-thumb" type="button" role="radio" aria-checked="false" data-world-thumb={w.id}>
                <span class={`wd-mini wd-${w.id}`} aria-hidden="true">
                  <i class="wd-glyph">Aa</i>
                  <i class="wd-l wd-l1"></i>
                  <i class="wd-l wd-l2"></i>
                  <i class="wd-l wd-l3"></i>
                  <i class="wd-tick"></i>
                </span>
                <span class="wd-name">{isES ? w.es : w.en}</span>
                <span class="wd-on" aria-hidden="true">{isES ? 'en uso' : 'active'}</span>
              </button>
            ))}
          </div>
        </section>

    {content.sections.map((section, si) => {
      const prevPhase = si > 0 ? content.sections[si - 1].phase : null;
      const phaseChanged = section.phase !== prevPhase;
      return (
        <section id={section.id} class="article-section" data-section>
          {phaseChanged && si > 0 && (
            <div class="phase-divider">
              <span>{section.phase}</span>
            </div>
          )}
          <h2 class="section-title">{section.title}</h2>
          <div class="section-body">
            {section.blocks.map((block) => (
              <BlockRenderer block={block} lang={lang} />
            ))}
          </div>
        </section>
      );
    })}

    <footer class="article-closing">
      <p class="closing-title">{content.closing.title}</p>
      {content.closing.paragraphs.map((p) => <p>{p}</p>)}
    </footer>
  </article>
</div>

<!-- World dock: takes over when the bench scrolls out of view -->
<div class="world-dock world-picker" data-world-dock hidden aria-label={isES ? 'Estilo visual' : 'Visual style'}>
  <div class="dock-popover" data-dock-popover hidden role="radiogroup">
    {WORLDS.map((w) => (
      <button class="world-thumb dock-thumb" type="button" role="radio" aria-checked="false" data-world-thumb={w.id}>
        <span class={`wd-mini wd-${w.id}`} aria-hidden="true">
          <i class="wd-glyph">Aa</i>
          <i class="wd-l wd-l1"></i>
          <i class="wd-l wd-l2"></i>
          <i class="wd-tick"></i>
        </span>
        <span class="wd-name">{isES ? w.es : w.en}</span>
      </button>
    ))}
  </div>
  <button class="dock-chip" type="button" data-dock-chip aria-expanded="false">
    <span class="dock-dot" data-dock-dot aria-hidden="true"></span>
    <span class="dock-label" data-dock-label>memo</span>
    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m18 15-6-6-6 6"/></svg>
  </button>
</div>

<script>
  import '../../scripts/article-interactions';
</script>

<style is:global>
  /* Anchor jumps (decision map -> inline cards) glide instead of teleporting */
  html { scroll-behavior: smooth; }

  /* ══ THE MEMO WORLD ═══════════════════════════════════════
     Light: a specification on laid paper. Dark: the same memo
     under a desk lamp. Courier Prime carries the document
     apparatus; Source Serif 4 carries the reading; one reference
     blue; hairline rules; paragraphs numbered in the gutter like
     the lines of a memo. */

  /* ── Shell ─────────────────────────────────────────── */
  .article-shell {
    display: flex;
    gap: 3.5rem;
    align-items: flex-start;
    width: 100%;
    max-width: 82rem;
    margin: 0 auto;
    min-width: 0;
  }
  .article { flex: 1; min-width: 0; counter-reset: ksec kpara kfig; }

  /* ── TOC rail: the memo's index ────────────────────── */
  .toc-rail { position: sticky; top: 5rem; flex: 0 0 auto; z-index: 30; }
  .toc-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    border-radius: 2px;
    border: 1px solid var(--rule);
    background: var(--paper-raised);
    padding: 0.4rem;
    overflow: hidden;
    /* no width transition: layout animation freezes in embedded browsers */
    width: 3rem;
  }
  .toc-card[data-expanded] {
    width: 21rem;
    align-items: stretch;
    padding: 0.75rem;
    max-height: calc(100vh - 7rem);
    overflow-y: auto;
  }
  .toc-expand {
    display: grid;
    place-items: center;
    width: 2.2rem;
    height: 2.2rem;
    border-radius: 2px;
    color: var(--ink-3);
    cursor: pointer;
    background: none;
    border: 1px solid transparent;
  }
  .toc-expand:hover { color: var(--accent); border-color: var(--rule); }
  .toc-card[data-expanded] .toc-expand { display: none; }
  .toc-dots {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.15rem;
    padding-top: 0.35rem;
  }
  .toc-card[data-expanded] .toc-dots { display: none; }
  .toc-dot {
    position: relative;
    display: grid;
    place-items: center;
    width: 1.9rem;
    height: 1.9rem;
    border-radius: 2px;
  }
  .toc-dot:hover { background: var(--paper-deep); }
  .dot-indicator {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 1px;
    background: var(--ink-3);
    transition: background 0.2s, box-shadow 0.2s;
  }
  .toc-dot:hover .dot-indicator { background: var(--ink-2); }
  .toc-dot.active .dot-indicator {
    background: var(--accent);
    box-shadow: 0 0 0 3px var(--accent-wash);
  }
  .toc-full { display: flex; flex-direction: column; gap: 1px; }
  .toc-full[hidden] { display: none; }
  .toc-full-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
    padding-bottom: 0.6rem;
    margin-bottom: 0.5rem;
    border-bottom: 1px solid var(--rule);
  }
  .toc-collapse {
    display: grid;
    place-items: center;
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 2px;
    color: var(--ink-3);
    cursor: pointer;
    background: none;
    border: none;
  }
  .toc-collapse:hover { color: var(--ink); background: var(--paper-deep); }
  .toc-tooltip {
    position: fixed;
    z-index: 60;
    padding: 0.3rem 0.6rem;
    border-radius: 2px;
    background: var(--ink);
    color: var(--paper);
    font-family: var(--font-app);
    font-size: 0.78rem;
    white-space: nowrap;
    pointer-events: none;
    transform: translateY(-50%);
    border: 1px solid var(--ink);
    max-width: 20rem;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .toc-heading {
    font-size: 0.72rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-2);
  }
  .toc-item {
    display: flex;
    flex-direction: column;
    gap: 1px;
    padding: 0.45rem 0.6rem;
    border-radius: 2px;
    font-size: 0.95rem;
    line-height: 1.35;
    color: var(--ink-2);
    text-decoration: none;
    transition: color 0.15s, background-color 0.15s;
  }
  .toc-item:hover { color: var(--ink); background: var(--paper-deep); }
  .toc-item.active { color: var(--accent); background: var(--accent-wash); }
  .toc-phase {
    font-size: 0.68rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--ink-2);
  }
  .toc-title { font-weight: 600; color: inherit; }
  @media (max-width: 71.99rem) {
    .toc-rail { display: none; }
  }

  /* ── Hero: the memo masthead ───────────────────────── */
  .hero { padding-bottom: 2.75rem; border-bottom: 1px solid var(--rule); margin-bottom: 2.75rem; }
  .memo-masthead {
    border: 1px solid var(--rule);
    border-radius: 2px;
    background: var(--paper-raised);
    padding: 0.85rem 1.1rem;
    margin-bottom: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }
  .mast-row {
    display: grid;
    grid-template-columns: 6.5rem 1fr;
    gap: 0.9rem;
    align-items: baseline;
  }
  .mast-key {
    font-family: var(--font-app);
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-2);
  }
  .mast-val {
    font-family: var(--font-app);
    font-size: 0.82rem;
    color: var(--ink-2);
    line-height: 1.5;
  }
  .hero-title {
    font-size: clamp(2rem, 4.6vw, 3.25rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 1.6rem;
    max-width: 44ch;
  }
  .hero-title-b { display: block; color: var(--accent); }
  .hero-prose { max-width: none; margin-bottom: 0.5rem; }
  .hero-prose p + p { margin-top: 1rem; }

  /* ── Sections: numbered like a memo's parts ────────── */
  .article-section { min-width: 0; scroll-margin-top: 5rem; }
  .article-section + .article-section { margin-top: 3.75rem; }
  .article .set-prose,
  .article .block-list {
    font-size: clamp(1.06rem, 0.64rem + 0.36vw, 1.26rem);
  }
  .phase-divider {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2.75rem;
  }
  .phase-divider::before,
  .phase-divider::after {
    content: '';
    height: 1px;
    background: var(--rule);
    flex: 1;
  }
  .phase-divider span {
    font-size: 0.72rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--ink-2);
    white-space: nowrap;
  }
  .section-title {
    counter-increment: ksec;
    font-size: clamp(1.5rem, 3vw, 1.95rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.2;
    margin-bottom: 1.4rem;
    color: var(--ink);
  }
  .section-title::before {
    content: '\00a7' counter(ksec) '\00a0\00a0';
    font-family: var(--font-app);
    font-weight: 700;
    font-size: 0.62em;
    color: var(--accent);
    letter-spacing: 0.06em;
    vertical-align: 0.28em;
  }
  .section-body { display: flex; flex-direction: column; gap: 1.2rem; }

  /* ── The traceability gutter: numbered paragraphs ──── */
  /* Desktop-only apparatus by decision: below 72rem the memo drops its
     gutter (the reading column is the priority); paragraph anchors for
     cross-references are recorded as future work in DESIGN.md. */
  @media (min-width: 72rem) {
    .article { padding-left: 3.4rem; }
    .article .set-prose > p {
      counter-increment: kpara;
      position: relative;
    }
    .article .set-prose > p::before {
      content: counter(kpara);
      position: absolute;
      left: -2.9rem;
      width: 2.2rem;
      text-align: right;
      font-family: var(--font-app);
      font-size: 0.68rem;
      line-height: 2.4;
      color: var(--ink-2);
    }
    .article .set-prose > p:hover::before { color: var(--accent); }
    /* The reading-guide panels are annotations, not memo body */
    .figure-guide .set-prose > p { counter-increment: none; }
    .figure-guide .set-prose > p::before { content: none; }
  }

  /* ── Blocks ────────────────────────────────────────── */
  .set-prose { line-height: 1.78; }
  .set-prose a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }
  .set-prose a:hover { color: var(--accent-strong); }
  .set-prose strong { color: inherit; font-weight: 700; }
  .block-h3 {
    font-size: 1.16rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-top: 1.35rem;
    margin-bottom: -0.3rem;
    color: var(--ink);
  }
  .block-list { padding-left: 1.35rem; display: flex; flex-direction: column; gap: 0.55rem; line-height: 1.72; }
  .block-list li::marker { color: var(--accent); }

  /* Cards: attachment blocks */
  .block-cards {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.9rem;
    margin: 0.5rem 0;
  }
  @media (min-width: 48rem) {
    .block-cards { grid-template-columns: repeat(3, 1fr); }
    .block-cards[data-cols='2'] { grid-template-columns: repeat(2, 1fr); }
  }
  .block-card {
    padding: 1.1rem 1.15rem;
    border-radius: 2px;
    border: 1px solid var(--rule);
    background: var(--paper-raised);
  }
  .block-card .tag {
    display: inline-block;
    font-size: 0.68rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--accent);
    margin-bottom: 0.5rem;
  }
  .block-card h4 { font-size: 1.02rem; font-weight: 700; margin-bottom: 0.5rem; color: var(--ink); }
  .block-card p { font-size: 0.98rem; line-height: 1.68; color: var(--ink-2); }

  /* Stats: the memo's measurements row */
  .block-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1px;
    border: 1px solid var(--rule);
    border-radius: 2px;
    overflow: hidden;
    background: var(--rule);
  }
  @media (min-width: 48rem) { .block-stats { grid-template-columns: repeat(4, 1fr); } }
  .stat-cell { padding: 1.1rem 1.15rem; background: var(--paper-raised); }
  .stat-value {
    font-family: var(--font-app);
    font-size: 1.45rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--accent);
    line-height: 1.15;
  }
  .stat-label { font-size: 0.95rem; line-height: 1.45; color: var(--ink-2); margin-top: 0.4rem; }

  /* Callouts: errata and margin notes of the memo */
  .block-callout {
    padding: 1.15rem 1.35rem;
    border-radius: 2px;
    margin: 0.5rem 0;
    border: 1px solid var(--rule);
    background: var(--paper-raised);
  }
  /* Errata boxes are apparatus: their text fills the box, not the 46rem column */
  .block-callout .set-prose { max-width: none; }
  .block-callout h4 {
    font-family: var(--font-app);
    font-size: 0.74rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    margin-bottom: 0.55rem;
    color: var(--ink-2);
  }
  .block-callout p { font-size: 1rem; line-height: 1.72; }
  .block-callout[data-tone='reference'] { background: var(--accent-wash); border-color: var(--accent); }
  .block-callout[data-tone='reference'] h4 { color: var(--accent); }
  .block-callout[data-tone='amber'] { background: var(--amber-wash); border-color: var(--amber); }
  .block-callout[data-tone='amber'] h4 { color: var(--amber); }
  .block-callout[data-tone='note'] h4::before { content: '\2014\00a0'; }

  /* Tables: ruled register pages */
  .block-table table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.98rem;
  }
  .block-table { border: 1px solid var(--rule); border-radius: 2px; overflow: hidden; background: var(--paper-raised); }
  .block-table .tbl-caption {
    font-size: 0.72rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--ink-2);
    padding: 0.85rem 1rem 0.55rem;
  }
  .block-table th, .block-table td {
    padding: 0.6rem 1rem;
    text-align: left;
    line-height: 1.5;
    border-top: 1px solid var(--rule);
  }
  .block-table thead th {
    font-size: 0.72rem;
    font-family: var(--font-app);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--ink-2);
    border-top: none;
    background: var(--paper-deep);
  }
  .block-table tbody th { font-weight: 600; white-space: nowrap; }

  /* Figures: plates pasted into the memo */
  .block-figure { counter-increment: kfig; margin: 1rem 0 0.5rem; }
  .block-figure img {
    width: 100%;
    border-radius: 2px;
    border: 1px solid var(--hairline);
    background: #ffffff;
    display: block;
  }
  .block-figure figcaption {
    font-size: 0.95rem;
    color: var(--ink-2);
    margin-top: 0.65rem;
    line-height: 1.6;
    cursor: default;
  }
  .block-figure figcaption::before {
    content: 'Fig. ' counter(kfig) ' \2014\00a0';
    font-family: var(--font-app);
    font-size: 0.8em;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: 0.04em;
  }
  /* Inline figure with lens magnifier: the diagram lives in the main thread */
  .figure-zoom { position: relative; cursor: crosshair; }
  .figure-zoom:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 4px;
    border-radius: 2px;
  }
  /* Figures embedded in prose (decision cards, guides) */
  .modal-figure { margin: 0.9rem 0 0.25rem; }
  .modal-figure img {
    width: 100%;
    border-radius: 2px;
    border: 1px solid var(--hairline);
    background: #ffffff;
    display: block;
  }
  .modal-figure figcaption {
    font-size: 0.95rem;
    color: var(--ink-2);
    margin-top: 0.5rem;
    line-height: 1.6;
  }
  dialog[data-doc-dialog] { max-width: min(76rem, calc(100vw - 1.5rem)); }
  .figure-lens {
    position: absolute;
    border-radius: 2px;
    border: 2px solid var(--accent);
    box-shadow: 0 8px 28px rgb(0 0 0 / 0.35), inset 0 0 0 1px rgb(0 0 0 / 0.2);
    background-repeat: no-repeat;
    background-color: #ffffff;
    pointer-events: none;
    z-index: 5;
  }
  .figure-lens-hint {
    position: absolute;
    transform: translateX(-50%);
    font-family: var(--font-app);
    font-size: 0.74rem;
    color: var(--paper);
    background: var(--ink);
    border: 1px solid var(--ink);
    border-radius: 2px;
    padding: 0.22rem 0.7rem;
    white-space: nowrap;
    pointer-events: none;
    z-index: 6;
  }
  .figure-guide-kicker {
    font-family: var(--font-app);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-weight: 700;
    color: var(--accent);
    margin-bottom: 0.3rem;
  }
  /* Reading guide: the annotation sheet clipped under its plate */
  .figure-guide {
    margin-top: 1rem;
    padding: 1.25rem 1.45rem;
    border-radius: 2px;
    border: 1px dashed var(--hairline);
    background: var(--paper-deep);
  }
  .figure-guide-title {
    font-size: 1.05rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--ink);
    margin-bottom: 0.6rem;
  }
  .figure-guide .set-prose { max-width: none; }
  .figure-guide .set-prose p { font-size: 0.98rem; line-height: 1.75; color: var(--ink-2); }
  .figure-guide .set-prose p + p { margin-top: 0.7rem; }
  .figure-guide .set-prose li { font-size: 0.98rem; line-height: 1.75; color: var(--ink-2); }
  .figure-guide .set-prose ul { margin: 0.55rem 0; padding-left: 1.3rem; list-style: disc; }
  .figure-guide .set-prose ol { margin: 0.55rem 0; padding-left: 1.5rem; list-style: decimal; }
  .figure-guide .set-prose ol ol { margin: 0.4rem 0; list-style: lower-alpha; }
  .figure-guide .set-prose li + li { margin-top: 0.45rem; }
  .figure-guide .set-prose strong { color: var(--ink); }

  /* ── Citation apparatus (doc refs) ──────────────────── */
  .doc-ref {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-family: var(--font-app);
    font-size: 0.78em;
    font-weight: 700;
    letter-spacing: 0.02em;
    color: var(--accent);
    background: var(--paper-raised);
    border: 1px solid var(--hairline);
    border-radius: 2px;
    padding: 0.08rem 0.45rem;
    cursor: pointer;
    vertical-align: baseline;
    white-space: nowrap;
  }
  .doc-ref:hover, .doc-ref:focus-visible { border-color: var(--accent); outline: none; }
  .modal-card.modal-card--doc {
    max-width: min(76rem, calc(100vw - 1.5rem));
    padding: 1.5rem 1.7rem;
  }
  .doc-prose {
    color: var(--ink-2);
    font-size: 1rem;
    line-height: 1.72;
  }
  .doc-prose h1, .doc-prose h2, .doc-prose h3, .doc-prose h4 {
    color: var(--ink);
    font-weight: 700;
    letter-spacing: -0.01em;
    margin: 1.6rem 0 0.6rem;
    line-height: 1.3;
  }
  .doc-prose h1 { font-size: 1.35rem; }
  .doc-prose h2 { font-size: 1.18rem; }
  .doc-prose h3, .doc-prose h4 { font-size: 1.05rem; }
  .doc-prose h1:first-child, .doc-prose h2:first-child, .doc-prose > p:first-child { margin-top: 0; }
  .doc-prose p { margin: 0.7rem 0; }
  .doc-prose ul, .doc-prose ol { margin: 0.7rem 0; padding-left: 1.5rem; }
  .doc-prose ul { list-style: disc; }
  .doc-prose ol { list-style: decimal; }
  .doc-prose li { margin: 0.35rem 0; }
  .doc-prose li > ul, .doc-prose li > ol { margin: 0.35rem 0; }
  .doc-prose a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }
  .doc-prose a:hover { color: var(--accent-strong); }
  .doc-prose strong { color: inherit; font-weight: 700; }
  .doc-prose em { font-style: italic; }
  .doc-prose blockquote {
    border-left: 1px solid var(--accent);
    padding-left: 1rem;
    margin: 0.9rem 0;
    color: var(--ink-2);
  }
  .doc-prose code {
    font-family: var(--font-app);
    font-size: 0.88em;
    color: var(--ink);
    background: var(--paper-deep);
    border: 1px solid var(--rule);
    border-radius: 2px;
    padding: 0.06rem 0.32rem;
  }
  .doc-prose table {
    border-collapse: collapse;
    margin: 1rem 0;
    font-size: 0.92rem;
    display: block;
    overflow-x: auto;
    max-width: 100%;
  }
  .doc-prose th, .doc-prose td {
    border: 1px solid var(--rule);
    padding: 0.45rem 0.7rem;
    text-align: left;
    vertical-align: top;
  }
  .doc-prose th { background: var(--paper-deep); font-weight: 700; color: var(--ink); font-family: var(--font-app); font-size: 0.85em; }
  .doc-prose img {
    max-width: 100%;
    border-radius: 2px;
    border: 1px solid var(--hairline);
    background: #ffffff;
    margin: 0.8rem 0;
  }
  .doc-prose hr { border: none; border-top: 1px solid var(--rule); margin: 1.4rem 0; }
  .doc-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.6rem 1rem;
    margin: 0.2rem 0 1.2rem;
    padding-bottom: 0.9rem;
    border-bottom: 1px solid var(--rule);
  }
  .doc-lang-note { font-size: 0.8rem; color: var(--ink-2); line-height: 1.5; font-style: italic; }
  .doc-footer-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
    margin-left: auto;
  }
  .doc-lang-toggle {
    font-family: var(--font-app);
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--accent);
    background: var(--paper-raised);
    border: 1px solid var(--hairline);
    border-radius: 2px;
    padding: 0.35rem 0.8rem;
    cursor: pointer;
    white-space: nowrap;
  }
  .doc-lang-toggle:hover, .doc-lang-toggle:focus-visible { border-color: var(--accent); outline: none; }
  .doc-original-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-app);
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--accent);
    text-decoration: none;
  }
  .doc-original-link:hover { text-decoration: underline; text-underline-offset: 3px; }

  /* ── Concept chips: defined terms of the glossary ──── */
  .concept-chip {
    display: inline;
    font-weight: 600;
    font-style: inherit;
    color: var(--ink);
    background: none;
    padding: 0;
    margin: 0;
    cursor: pointer;
    border-bottom: 1px dotted var(--accent);
  }
  .concept-chip:hover, .concept-chip:focus-visible { color: var(--accent); outline: none; }

  /* ── Decision card: a status block of the memo ─────── */
  .decision-card {
    padding: 1.35rem 1.45rem;
    border-radius: 2px;
    border: 1px solid var(--rule);
    border-top: 3px double var(--ink-3);
    background: var(--paper-raised);
    margin: 0.75rem 0;
    scroll-margin-top: 2rem;
  }
  .decision-card h4 {
    font-size: 1.08rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-bottom: 0.95rem;
    color: var(--ink);
  }
  .decision-card dl { display: flex; flex-direction: column; gap: 0.75rem; }
  .decision-card dt {
    font-family: var(--font-app);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--accent);
    margin-bottom: 2px;
  }
  .decision-card dd { font-size: 0.98rem; line-height: 1.68; color: var(--ink-2); }
  .decision-card dd strong { color: var(--ink); }

  /* ── Decision map: the memo's index of decisions ───── */
  .decision-map { display: flex; flex-direction: column; gap: 2.25rem; }
  .pillar + .pillar { margin-top: 0.5rem; }
  .pillar-head { margin-bottom: 1rem; }
  .pillar-head h3 { font-size: 1.14rem; font-weight: 700; color: var(--ink); }
  .pillar-head p { font-size: 0.98rem; color: var(--ink-2); line-height: 1.6; margin-top: 0.3rem; }
  .pillar-grid { display: flex; flex-direction: column; gap: 0.9rem; }
  .map-entry {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.95rem 1.2rem;
    border-radius: 2px;
    border: 1px solid var(--rule);
    background: var(--paper-raised);
    cursor: pointer;
    width: 100%;
    text-align: left;
    font: inherit;
    color: inherit;
    text-decoration: none;
  }
  .map-entry:hover { border-color: var(--accent); }
  .map-entry .entry-title { font-size: 1rem; font-weight: 600; line-height: 1.4; color: var(--ink); }
  .map-entry .entry-adrs {
    font-family: var(--font-app);
    font-size: 0.8rem;
    color: var(--ink-2);
    white-space: nowrap;
  }

  /* ── Context diagram: the memo's plate zero ────────── */
  .context-diagram {
    border: 1px solid var(--rule);
    border-radius: 2px;
    padding: 1.4rem 1.2rem;
    background: var(--paper-raised);
    margin: 0.5rem 0;
  }
  .cd-title {
    font-family: var(--font-app);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-2);
    margin-bottom: 1.1rem;
    text-align: center;
  }
  .cd-col-title {
    font-family: var(--font-app);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--ink-2);
    margin-bottom: 0.5rem;
    text-align: center;
  }
  .cd-flow {
    display: grid;
    grid-template-columns: 1.1fr auto minmax(13rem, auto) auto 1.1fr;
    gap: 1.25rem;
    align-items: center;
  }
  .cd-flow .cd-stack:first-child { grid-column: 1; }
  .cd-flow .cd-arrows:first-of-type { grid-column: 2; }
  .cd-central { grid-column: 3; }
  .cd-flow .cd-arrows:last-of-type { grid-column: 4; }
  .cd-flow .cd-stack:last-child { grid-column: 5; }
  @media (max-width: 40rem) {
    .cd-flow { grid-template-columns: 1fr; }
    .cd-flow .cd-stack:first-child,
    .cd-flow .cd-arrows:first-of-type,
    .cd-central,
    .cd-flow .cd-arrows:last-of-type,
    .cd-flow .cd-stack:last-child { grid-column: 1; }
    .cd-arrows { justify-content: center; }
    .cd-arrows svg { transform: rotate(90deg); }
  }
  .cd-stack { display: flex; flex-direction: column; gap: 0.5rem; }
  .cd-node {
    padding: 0.55rem 0.8rem;
    border-radius: 2px;
    border: 1px solid var(--hairline);
    font-size: 0.95rem;
    text-align: center;
    background: var(--paper-deep);
    color: var(--ink-2);
  }
  .cd-central {
    padding: 1.4rem 1.5rem;
    border-radius: 2px;
    border: 1.5px solid var(--accent);
    background: var(--accent-wash);
    text-align: center;
  }
  .cd-central strong { font-size: 1rem; display: block; line-height: 1.35; color: var(--ink); }
  .cd-central span { font-size: 0.95rem; color: var(--ink-2); display: block; margin-top: 0.3rem; }
  .cd-arrows { display: flex; flex-direction: column; gap: 0.4rem; align-items: center; color: var(--accent); }

  /* ── Closing: the memo's sign-off ──────────────────── */
  .article-closing {
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 1px solid var(--rule);
    font-size: 0.98rem;
    color: var(--ink-2);
    line-height: 1.72;
  }
  .closing-title {
    font-family: var(--font-app);
    font-size: 0.74rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-2);
    margin-bottom: 0.5rem;
  }

  /* ── Modals: sheets pulled from the file ───────────── */
  .modal-shell {
    margin: auto;
    padding: 0;
    border: none;
    background: transparent;
    max-width: min(40rem, calc(100vw - 2rem));
    max-height: calc(100dvh - 3rem);
  }
  .modal-shell::backdrop {
    background: rgb(15 13 9 / 0.62);
    backdrop-filter: blur(3px);
  }
  .modal-card {
    position: relative;
    background: var(--paper-raised);
    color: var(--ink-2);
    border: 1px solid var(--hairline);
    border-radius: 2px;
    padding: 1.75rem 1.9rem;
    box-shadow: var(--shadow-lift);
    overflow-y: auto;
    max-height: calc(100dvh - 3rem);
  }
  .modal-close {
    position: absolute;
    top: 0.9rem;
    right: 0.9rem;
    width: 2rem;
    height: 2rem;
    display: grid;
    place-items: center;
    border-radius: 2px;
    border: 1px solid var(--rule);
    background: transparent;
    color: var(--ink-3);
    cursor: pointer;
  }
  .modal-close:hover { color: var(--ink); border-color: var(--accent); }
  .modal-title {
    font-size: 1.2rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    padding-right: 2.5rem;
    margin-bottom: 1.1rem;
    color: var(--ink);
  }
  .modal-body p,
  .modal-body li {
    font-size: 1rem;
    line-height: 1.72;
    color: var(--ink-2);
  }
  .modal-body p + p { margin-top: 0.8rem; }
  .modal-body ul { margin: 0.5rem 0; padding-left: 1.25rem; list-style: disc; }
  .modal-body li + li { margin-top: 0.45rem; }
  .modal-body strong { color: var(--ink); }
  /* ── Lens stage: the diagram fills the screen, scroll visibly locked ── */
  .lens-stage {
    position: fixed;
    inset: 0;
    z-index: 90;
    background: rgb(15 13 9 / 0.72);
    backdrop-filter: blur(3px);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.85rem;
    padding: 2rem;
  }
  .lens-stage[hidden] { display: none; }
  .lens-stage-plate {
    position: relative;
    cursor: crosshair;
    max-width: 100%;
  }
  .lens-stage-img {
    display: block;
    /* the plate takes the full screen height, with padding to breathe */
    max-height: calc(100dvh - 8.5rem);
    max-width: calc(100vw - 4rem);
    width: auto;
    height: auto;
    border: 1px solid var(--hairline);
    border-radius: 2px;
    background: #ffffff;
  }
  .lens-stage-bar {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    font-family: var(--font-app);
    font-size: 0.78rem;
    color: #e9e1cb;
  }
  .lens-stage-help { opacity: 0.85; }
  .lens-stage-close {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-family: var(--font-app);
    font-size: 0.78rem;
    font-weight: 700;
    color: #e9e1cb;
    background: none;
    border: 1px solid rgb(233 225 203 / 0.4);
    border-radius: 2px;
    padding: 0.3rem 0.7rem;
    cursor: pointer;
  }
  .lens-stage-close:hover { border-color: #e9e1cb; }
  .lens-stage-close:focus-visible { outline: 2px solid #a3b8f3; outline-offset: 2px; }

  /* ADR citation row under each inline decision card */
  .decision-adrs {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1.25rem;
    padding-top: 1.1rem;
    border-top: 1px solid var(--rule);
  }

  /* ══ World bench & dock: the style comparison instrument ════════════
     Its chrome is deliberately fixed (does not re-skin with the worlds):
     only the miniatures depict each world. */

  /* Instant switching while previewing: no crossfades to freeze */
  html.world-snap *, html.world-snap *::before, html.world-snap *::after {
    transition: none !important;
  }

  .world-bench {
    border: 1px solid #2a2620;
    background: #1c1915;
    padding: 1rem 1.1rem 1.1rem;
    margin: 0.5rem 0 2.75rem;
  }
  /* While comparing, the bench detaches from the flow (inline fixed
     position set by JS) so no world can move it under the cursor. */
  .world-bench.is-frozen {
    box-shadow: 0 18px 50px rgb(0 0 0 / 0.5);
  }
  :root:not(.dark) .world-bench { border-color: #c9c0aa; background: #f8f4e9; }
  .bench-legend {
    display: flex;
    align-items: baseline;
    gap: 0.9rem;
    margin: 0 0 0.8rem;
  }
  .bench-title {
    font-family: 'Courier Prime', monospace;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: #e9e1cb;
  }
  :root:not(.dark) .bench-title { color: #241f14; }
  .bench-hint {
    font-family: 'Courier Prime', monospace;
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    color: #8d8368;
  }
  .bench-row {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 0.6rem;
  }
  @media (max-width: 64rem) { .bench-row { grid-template-columns: repeat(3, 1fr); } }
  @media (max-width: 26rem) { .bench-row { grid-template-columns: repeat(2, 1fr); } }

  .world-thumb {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: 0;
    border: 1px solid transparent;
    background: none;
    cursor: pointer;
    font: inherit;
    text-align: center;
    position: relative;
  }
  .world-thumb:focus-visible { outline: 2px solid #e9e1cb; outline-offset: 2px; }
  :root:not(.dark) .world-thumb:focus-visible { outline-color: #241f14; }
  .world-thumb:hover .wd-mini, .world-thumb:focus-visible .wd-mini {
    transform: translateY(-2px);
    box-shadow: 0 6px 18px rgb(0 0 0 / 0.35);
  }
  .wd-mini {
    display: block;
    position: relative;
    aspect-ratio: 5 / 3;
    border: 1px solid #2a2620;
    transition: transform 0.12s ease, box-shadow 0.12s ease;
    overflow: hidden;
  }
  :root:not(.dark) .wd-mini { border-color: #b6ac93; }
  .wd-name {
    font-family: 'Courier Prime', monospace;
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    color: #b1a78d;
  }
  :root:not(.dark) .wd-name { color: #6e6651; }
  .wd-on {
    display: none;
    position: absolute;
    top: 0.3rem;
    right: 0.3rem;
    font-family: 'Courier Prime', monospace;
    font-size: 0.56rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #e9e1cb;
    background: rgb(0 0 0 / 0.55);
    padding: 0.1rem 0.3rem;
  }
  :root:not(.dark) .wd-on { color: #241f14; background: rgb(255 255 255 / 0.75); }
  .world-thumb[data-active] { border-color: #e9e1cb; }
  :root:not(.dark) .world-thumb[data-active] { border-color: #241f14; }
  .world-thumb[data-active] .wd-on { display: block; }
  .world-thumb[data-active] .wd-name { color: #e9e1cb; font-weight: 700; }
  :root:not(.dark) .world-thumb[data-active] .wd-name { color: #241f14; }

  /* Greeked text lines + glyph, painted per world */
  .wd-l { position: absolute; left: 12%; height: 2px; border-radius: 0; }
  .wd-l1 { width: 62%; top: 58%; }
  .wd-l2 { width: 48%; top: 70%; }
  .wd-l3 { width: 54%; top: 82%; }
  .wd-glyph {
    position: absolute;
    top: 10%;
    left: 12%;
    font-size: 1.05rem;
    line-height: 1;
    font-style: normal;
  }
  .wd-tick { position: absolute; }

  .wd-memo { background: #f4efe3; }
  .wd-memo .wd-glyph { font-family: 'Source Serif 4', serif; font-weight: 800; color: #241f14; }
  .wd-memo .wd-l { background: #6e6651; }
  .wd-memo .wd-l1 { background: #1e42a8; }
  .wd-memo .wd-tick { top: 14%; right: 12%; width: 14px; height: 3px; background: #1e42a8; }

  .wd-botteghe { background: #12305e; }
  .wd-botteghe::before {
    content: '';
    position: absolute;
    inset: 14% 10% 12% 22%;
    background: #f6f1e2;
  }
  .wd-botteghe .wd-glyph { font-family: Cardo, serif; font-weight: 700; color: #221d11; z-index: 1; }
  .wd-botteghe .wd-l { left: 24%; background: #544c38; z-index: 1; }
  .wd-botteghe .wd-l1 { background: #a83317; }
  .wd-botteghe .wd-tick { top: 16%; right: 13%; width: 3px; height: 14px; background: #e8745a; }

  .wd-bench { background: #050505; }
  .wd-bench .wd-glyph {
    font-family: 'Barlow Condensed', sans-serif;
    font-weight: 600;
    color: #17130d;
    background: #f3ead8;
    padding: 1px 4px;
    font-size: 0.9rem;
  }
  .wd-bench .wd-l { background: #f3ead8; height: 8px; opacity: 0.9; }
  .wd-bench .wd-l1 { width: 74%; top: 56%; }
  .wd-bench .wd-l2 { width: 74%; top: 68%; }
  .wd-bench .wd-l3 { width: 74%; top: 80%; }
  .wd-bench .wd-tick { top: 12%; right: 12%; width: 8px; height: 8px; background: #ff6a1a; }

  .wd-plate { background: #f2f0ea; }
  .wd-plate::before {
    content: '';
    position: absolute;
    inset: 16% 18% 20% 10%;
    background: #fbfaf7;
    border: 1.5px solid #2b313c;
    box-shadow: 4px 4px 0 #2b313c;
  }
  .wd-plate .wd-glyph { font-family: 'Allerta Stencil', sans-serif; color: #b04a24; z-index: 1; }
  .wd-plate .wd-l { left: 13%; background: #565e6c; z-index: 1; }
  .wd-plate .wd-tick { top: 10%; right: 10%; width: 10px; height: 10px; border: 1.5px solid #2b313c; }

  .wd-tensegrity { background: #d9d6cf; }
  .wd-tensegrity .wd-glyph { font-family: Archivo, sans-serif; font-weight: 700; color: #202022; }
  .wd-tensegrity .wd-l { background: #4d4d50; height: 3px; }
  .wd-tensegrity .wd-l::before { content: none; }
  .wd-tensegrity .wd-tick {
    top: 46%; left: 38%;
    width: 9px; height: 9px;
    background: #ab0d28;
    box-shadow: 14px 12px 0 -3px #202022, 28px 24px 0 -3px #202022;
  }

  .wd-datamatics { background: #ffffff; }
  .wd-datamatics .wd-glyph {
    font-family: 'Courier Prime', monospace;
    font-weight: 700;
    color: #ffffff;
    background: #000000;
    padding: 1px 4px;
    font-size: 0.85rem;
  }
  .wd-datamatics .wd-l { background: #000000; }
  .wd-datamatics .wd-tick { top: 0; left: 0; right: 0; height: 3px; background: #000000; }
  .dark .wd-datamatics { background: #000000; }
  .dark .wd-datamatics .wd-l { background: #ffffff; }
  .dark .wd-datamatics .wd-glyph { color: #000000; background: #ffffff; }
  .dark .wd-datamatics .wd-tick { background: #ffffff; }

  /* ── Floating dock ── */
  .world-dock {
    position: fixed;
    right: 1.25rem;
    bottom: 1.25rem;
    z-index: 70;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.5rem;
  }
  .world-dock[hidden] { display: none; }
  .dock-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    min-width: 9.5rem;
    font-family: 'Courier Prime', monospace;
    font-size: 0.75rem;
    font-weight: 700;
    color: #e9e1cb;
    background: #1c1915;
    border: 1px solid #2a2620;
    border-radius: 2px;
    padding: 0.45rem 0.75rem;
    cursor: pointer;
    box-shadow: 0 8px 24px rgb(0 0 0 / 0.4);
  }
  :root:not(.dark) .dock-chip { color: #241f14; background: #f8f4e9; border-color: #c9c0aa; }
  .dock-chip:hover { border-color: #8d8368; }
  .dock-dot {
    width: 9px;
    height: 9px;
    border-radius: 1px;
    background: #a3b8f3;
  }
  .dock-popover {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.5rem;
    background: #1c1915;
    border: 1px solid #2a2620;
    border-radius: 2px;
    padding: 0.7rem;
    box-shadow: 0 14px 40px rgb(0 0 0 / 0.5);
    animation: dock-pop 0.15s ease-out;
  }
  :root:not(.dark) .dock-popover { background: #f8f4e9; border-color: #c9c0aa; }
  .dock-popover[hidden] { display: none; }
  .dock-thumb .wd-mini { aspect-ratio: 5 / 3; }
  @media (prefers-reduced-motion: reduce) {
    .dock-popover { animation: none; }
    .world-thumb:hover .wd-mini { transform: none; }
  }
  @keyframes dock-pop {
    from { opacity: 0; transform: translateY(6px) scale(0.98); }
    to { opacity: 1; transform: none; }
  }
</style>
